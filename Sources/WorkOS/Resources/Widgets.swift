// This file is auto-generated by oagen. Do not edit.

import Foundation

/// Operations for the Widgets API.
public struct Widgets: Sendable {
    let transport: Transport

    /// Generate a widget token
    ///
    /// Generate a widget token for a user, optionally scoped to an organization. When an organization is specified, org-scoped widgets are enabled; omitting it issues a user-only token for widgets like `UserProfile` and `UserSecurity`.
    public func createToken(
        organizationId: String? = nil,
        userId: String? = nil,
        scopes: [WidgetSessionTokenScopes]? = nil,
        requestOptions: RequestOptions? = nil
    ) async throws -> WidgetSessionTokenResponse {
        let path = "widgets/token"
        var body = EncodableBody()
        body.set("organization_id", organizationId)
        body.set("user_id", userId)
        body.set("scopes", scopes)
        return try await transport.request(
            method: "POST",
            path: path,
            query: [],
            body: body,
            options: requestOptions,
            as: WidgetSessionTokenResponse.self
        )
    }
}
