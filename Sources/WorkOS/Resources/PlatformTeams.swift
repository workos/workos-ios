// This file is auto-generated by oagen. Do not edit.

import Foundation

/// Operations for the PlatformTeams API.
public struct PlatformTeams: Sendable {
    let transport: Transport

    /// Create a team
    ///
    /// Creates a team along with its default project, a staging environment, and a production environment. An admin invitation is sent to `admin_email`, onboarding is marked complete with AuthKit enabled, and the calling platform is authorized to act inside the team.
    ///
    /// - Parameter adminEmail: The email address of the person who will administer the team. An invitation is sent to this address.
    /// - Parameter name: Name of the team.
    /// - Parameter requestOptions: Per-request overrides (idempotency key, API key, headers, timeout).
    public func createTeam(
        adminEmail: String,
        name: String,
        requestOptions: RequestOptions? = nil
    ) async throws -> Team {
        let path = "platform/teams"
        var body = EncodableBody()
        body.set("admin_email", adminEmail)
        body.set("name", name)
        return try await transport.request(
            method: "POST",
            path: path,
            query: [],
            body: body,
            options: requestOptions,
            as: Team.self
        )
    }

    /// Get a team
    ///
    /// Returns a team, and doubles as a health check on the platform's access to it. Read `production_state` before attempting to create a production environment.
    ///
    /// - Parameter teamId: The ID of the team.
    /// - Parameter requestOptions: Per-request overrides (idempotency key, API key, headers, timeout).
    public func getTeam(
        teamId: String,
        requestOptions: RequestOptions? = nil
    ) async throws -> Team {
        let path = "platform/teams/\(PathEncoding.segment(teamId))"
        return try await transport.request(
            method: "GET",
            path: path,
            query: [],
            body: nil,
            options: requestOptions,
            as: Team.self
        )
    }
}
