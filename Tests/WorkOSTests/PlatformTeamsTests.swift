// This file is auto-generated by oagen. Do not edit.

import Foundation
import Testing

@testable import WorkOS

/// Wire-level tests for the PlatformTeams resource: each test performs a real
/// call through the mocked transport and asserts the request that went out
/// and the decoded response that came back.
@Suite struct PlatformTeamsTests {
    @Test func resourceIsReachable() {
        let (client, _) = makeTestClient()
        _ = client.platformTeams
        #expect(client.configuration.apiKey == "sk_test_123")
    }

    @Test func createTeamSendsExpectedRequest() async throws {
        let (client, recorder) = makeTestClient(
            responding:
                #"{"object":"team","id":"team_01JX9AN6E02HAG2Q2CKGC1XT5W","name":"Example Team","production_state":"Active","production_enabled_at":"2024-01-01T00:00:00.000Z","created_at":"2024-01-01T00:00:00.000Z","updated_at":"2024-01-01T00:00:00.000Z"}"#
        )
        let result = try await client.platformTeams.createTeam(
            adminEmail: "test_admin_email", name: "test_name")

        let request = try #require(recorder.lastRequest)
        #expect(request.httpMethod == "POST")
        #expect(request.url?.path == "/platform/teams")
        let body = try #require(recorder.lastBody)
        let json = try JSONSerialization.jsonObject(with: body) as? [String: Any]
        #expect(json?["admin_email"] != nil)
        #expect(result.id == "team_01JX9AN6E02HAG2Q2CKGC1XT5W")
    }

    @Test func getTeamSendsExpectedRequest() async throws {
        let (client, recorder) = makeTestClient(
            responding:
                #"{"object":"team","id":"team_01JX9AN6E02HAG2Q2CKGC1XT5W","name":"Example Team","production_state":"Active","production_enabled_at":"2024-01-01T00:00:00.000Z","created_at":"2024-01-01T00:00:00.000Z","updated_at":"2024-01-01T00:00:00.000Z"}"#
        )
        let result = try await client.platformTeams.getTeam(teamId: "sample-team-id")

        let request = try #require(recorder.lastRequest)
        #expect(request.httpMethod == "GET")
        #expect(request.url?.path == "/platform/teams/sample-team-id")
        #expect(result.id == "team_01JX9AN6E02HAG2Q2CKGC1XT5W")
    }
}
